---
- name: Run Proxy Container
  docker_container:
    name: httpd
    image: madow78/tp-devops-httpd
    pull: true
    networks:
      - name: app-network
    ports:
      - "80:80"
    state: started
    restart_policy: always
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python
